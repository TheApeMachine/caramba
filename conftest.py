"""Pytest configuration.

Tests should import the package namespace (`caramba.*`) rather than relying on
repo-root "flat" imports (e.g. `import runtime`, `from config import ...`).
"""

from __future__ import annotations
