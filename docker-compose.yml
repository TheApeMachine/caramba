services:
  webcrawl-mcp:
    build:
      context: .
      dockerfile: ai/tools/webcrawl/Dockerfile
    ports:
      - "8001:8001"
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  falkordb:
    image: falkordb/falkordb:latest
    ports:
      - "6379:6379"
      - "3000:3000"
    environment:
      - FALKORDB_PASSWORD=${FALKORDB_PASSWORD:-}
      - BROWSER=${BROWSER:-1}
    volumes:
      - falkordb_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6379", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  graphiti-mcp:
    image: zepai/knowledge-graph-mcp:standalone
    env_file:
      - path: .env
        required: true
    depends_on:
      falkordb:
        condition: service_healthy
    environment:
      - FALKORDB_URI=${FALKORDB_URI:-redis://falkordb:6379}
      - FALKORDB_PASSWORD=${FALKORDB_PASSWORD:-}
      - FALKORDB_DATABASE=${FALKORDB_DATABASE:-main}
      - GRAPHITI_GROUP_ID=${GRAPHITI_GROUP_ID:-main}
      - SEMAPHORE_LIMIT=${SEMAPHORE_LIMIT:-10}
      - CONFIG_PATH=/app/mcp/config/config.yaml
      - PATH=/root/.local/bin:${PATH}
      - MAX_REFLEXION_ITERATIONS=0
    volumes:
      - ./falkordb-config.yml:/app/mcp/config/config.yaml:ro
    ports:
      - "8002:8000"
    command: ["uv", "run", "main.py"]

  sympy-mcp:
    image: ghcr.io/sdiehl/sympy-mcp:main
    ports:
      - "8081:8081"

  # paper-tool: disabled - deprecated service
  # paper-tool:
  #   build:
  #     context: .
  #     dockerfile: docker/Dockerfile.paper-tool
  #   env_file:
  #     - path: .env
  #       required: true
  #   environment:
  #     - PAPER_PATH=${PAPER_PATH:-/app/artifacts}
  #     - PATH=/root/.local/bin:${PATH}
  #   volumes:
  #     - ./artifacts:/app/artifacts
  #   ports:
  #     - "8003:8001"
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
  #     interval: 10s
  #     timeout: 5s
  #     start_period: 15s
  #     retries: 3

  deeplake-mcp:
    build:
      context: .
      dockerfile: ai/tools/deeplake/Dockerfile
    env_file:
      - path: .env
        required: true
    environment:
      - MCP_SERVER_HOST=0.0.0.0
      - MCP_SERVER_PORT=8001
      # Persist DeepLake dataset inside the mounted volume.
      - DEEPLAKE_DATASET_DIR=/app/.caramba/deeplake_colbert
      # Optional overrides:
      # - DEEPLAKE_DATASET_URI=file:///app/.caramba/deeplake_colbert
      # - DEEPLAKE_MODEL_NAME=colbert-ir/colbertv2.0
      # - DEEPLAKE_COLBERT_ROOT=/app/experiments
      # - DEEPLAKE_CREATE_IF_MISSING=1
      - PATH=/root/.local/bin:${PATH}
    ports:
      - "8004:8001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      start_period: 30s
      retries: 3

  filesystem-mcp:
    build:
      context: .
      dockerfile: ai/tools/filesystem/Dockerfile
    ports:
      - "8005:8001"
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - FILESYSTEM_ALLOWED_ROOTS=/app/artifacts,/app/config
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  codegraph-mcp:
    build:
      context: .
      dockerfile: ai/tools/codegraph/Dockerfile
    depends_on:
      falkordb:
        condition: service_healthy
    ports:
      - "8006:8001"
    command:
      [
        "python",
        "-m",
        "ai.tools.codegraph.tool",
        "--host",
        "0.0.0.0",
        "--port",
        "8001",
        "--falkordb-uri",
        "redis://falkordb:6379",
        "--graph-name",
        "caramba_code"
      ]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  # Persona A2A servers (expert agents)
  architect:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/architect.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  developer:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/developer.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  research-lead:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/research_lead.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  writer:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/writer.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  mathematician:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/mathematician.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  ml-expert:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/ml_expert.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  reviewer:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/reviewer.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  verifier:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/verifier.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  knowledge-curator:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/knowledge_curator.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  context-compactor:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/context_compactor.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  note-taker:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/note_taker.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  catalyst:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/catalyst.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  feature-analyst:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/feature_analyst.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  code-reviewer:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/code_reviewer.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  experiment-proposer:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/experiment_proposer.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  results-analyzer:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/results_analyzer.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

  # Root agent (exposed to host for TUI connection)
  root-agent:
    build:
      context: .
      dockerfile: docker/Dockerfile.persona-a2a
    env_file:
      - path: .env
        required: true
    volumes:
      - ./artifacts:/app/artifacts
      - ./config:/app/config:ro
    environment:
      - PERSONA_FILE=config/personas/root.yml
      - PORT=8001
      - CARAMBA_GRAPHITI_HOST=graphiti-mcp
      - CARAMBA_GRAPHITI_PORT=8000
      - CARAMBA_DEEPLAKE_HOST=deeplake-mcp
      - CARAMBA_DEEPLAKE_PORT=8001
      - CARAMBA_FILESYSTEM_HOST=filesystem-mcp
      - CARAMBA_FILESYSTEM_PORT=8001
      - CARAMBA_CODEGRAPH_HOST=codegraph-mcp
      - CARAMBA_CODEGRAPH_PORT=8001
      - CARAMBA_WEBCRAWL_HOST=webcrawl-mcp
      - CARAMBA_WEBCRAWL_PORT=8001
    ports:
      - "9000:8001"  # Expose root agent to host for TUI
    depends_on:
      - architect
      - developer
      - research-lead
      - writer
      - mathematician
      - ml-expert
      - reviewer
      - verifier
      - knowledge-curator
      - context-compactor
      - note-taker
      - catalyst
      - feature-analyst
      - code-reviewer
      - experiment-proposer
      - results-analyzer
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/.well-known/agent-card.json"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 3

volumes:
  falkordb_data:
    driver: local