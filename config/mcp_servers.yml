graphiti:
  transport: streamable-http
  url: "http://localhost:8002/mcp"

paper:
  transport: streamable-http
  url: "http://localhost:8001/mcp"

# Alias kept for backwards compatibility with existing personas.
paper-tools:
  transport: streamable-http
  url: "http://localhost:8001/mcp"

deeplake:
  transport: streamable-http
  url: "http://localhost:8004/mcp"

# Hosted by Weights & Biases (W&B). Requires WANDB_API_KEY in the environment.
wandb:
  transport: streamable-http
  url: "https://mcp.withwandb.com/mcp"
  headers:
    authorization: "Bearer ${WANDB_API_KEY}"

