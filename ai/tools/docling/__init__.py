"""Docling MCP tool package

Provides an MCP server that converts documents (especially PDFs) into text/markdown/JSON
using Docling. This lets agents read and reason over PDFs without embedding raw binary
content directly into prompts.
"""

