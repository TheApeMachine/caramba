package agent

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestNewBuilder(t *testing.T) {
	Convey("Given a builder", t, func() {
		builder := NewBuilder()

		So(builder, ShouldNotBeNil)
	})
}

func TestHandleTask(t *testing.T) {
	Convey("Given a builder", t, func() {
		builder := NewBuilder()

		So(builder, ShouldNotBeNil)
	})
}

func TestWithCard(t *testing.T) {
	Convey("Given a builder", t, func() {
		builder := NewBuilder()

		So(builder, ShouldNotBeNil)
	})
}

func TestWithA2AClient(t *testing.T) {
	Convey("Given a builder", t, func() {
		builder := NewBuilder()

		So(builder, ShouldNotBeNil)
	})
}

func TestWithMCPClient(t *testing.T) {
	Convey("Given a builder", t, func() {
		builder := NewBuilder()

		So(builder, ShouldNotBeNil)
	})
}

func TestWithSubAgents(t *testing.T) {
	Convey("Given a builder", t, func() {
		builder := NewBuilder()

		So(builder, ShouldNotBeNil)
	})
}

func TestWithMemory(t *testing.T) {
	Convey("Given a builder", t, func() {
		builder := NewBuilder()

		So(builder, ShouldNotBeNil)
	})
}

func TestWithTaskManager(t *testing.T) {
	Convey("Given a builder", t, func() {
		builder := NewBuilder()

		So(builder, ShouldNotBeNil)
	})
}
