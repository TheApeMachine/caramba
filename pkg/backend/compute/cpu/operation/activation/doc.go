/*
Package activation defines CPU activation maps as operation.Operation implementations.

Import as:

	import "github.com/theapemachine/caramba/backend/compute/cpu/operation/activation"

SwiGLU expects input of length 2·n (gates concatenated with values) and returns length n.
*/
package activation
