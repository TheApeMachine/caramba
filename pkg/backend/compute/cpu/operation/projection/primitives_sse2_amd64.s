#include "textflag.h"

// projMatmulSSE2(dst, a, b []float64, M, K, N int)
TEXT ·projMatmulSSE2(SB), NOSPLIT, $0-96
	MOVQ dst+0(FP), R8
	MOVQ a+24(FP), R9
	MOVQ b+48(FP), R10
	MOVQ M+72(FP), R11
	MOVQ K+80(FP), R12
	MOVQ N+88(FP), R13

	XORQ R14, R14
outer_i2:
	CMPQ R14, R11
	JGE  done_mm2

	XORQ R15, R15
middle_j2:
	MOVQ R13, CX
	SUBQ $2, CX
	CMPQ R15, CX
	JG   tail_j2

	XORPS X0, X0

	XORQ AX, AX
inner_k2:
	MOVQ R14, BX
	IMULQ R12, BX
	ADDQ AX, BX
	MOVSD (R9)(BX*8), X15
	UNPCKLPD X15, X15

	MOVQ AX, BX
	IMULQ R13, BX
	ADDQ R15, BX
	MOVUPD (R10)(BX*8), X1

	MULPD  X15, X1
	ADDPD  X1, X0

	INCQ AX
	CMPQ AX, R12
	JL   inner_k2

	MOVQ R14, BX
	IMULQ R13, BX
	ADDQ R15, BX
	MOVUPD X0, (R8)(BX*8)

	ADDQ $2, R15
	JMP  middle_j2

tail_j2:
	CMPQ R15, R13
	JGE  next_i2
	XORPS X0, X0
	XORQ AX, AX
inner_k_scalar2:
	MOVQ R14, BX
	IMULQ R12, BX
	ADDQ AX, BX
	MOVSD (R9)(BX*8), X1
	MOVQ AX, BX
	IMULQ R13, BX
	ADDQ R15, BX
	MOVSD (R10)(BX*8), X2
	MULSD X2, X1
	ADDSD X1, X0
	INCQ AX
	CMPQ AX, R12
	JL   inner_k_scalar2
	MOVQ R14, BX
	IMULQ R13, BX
	ADDQ R15, BX
	MOVSD X0, (R8)(BX*8)
	INCQ R15
	JMP  tail_j2

next_i2:
	INCQ R14
	JMP  outer_i2

done_mm2:
	RET
