"""Adapters.

Adapters transform one representation into another by composing reusable steps.
They do not encode external schemas directly; they consume schema objects and
policy objects supplied by configuration.
"""
from __future__ import annotations
